This is a Script that incoludes a Rolemenu with the following features:

- a button to give all role in the select menu (edit the id)
- a button to remove all yourself roles (edit the id)
- a select menu to choose role
